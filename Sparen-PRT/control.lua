require "util"
require "defines"

--Initialize global variables that are handled on loading a save file here.
game.oninit(function() 

    end
)

game.onevent(defines.events.ontick, function(event)
    --If the player is in a PRT Pod, do XYZ
    --if game.player.character and game.player.character.vehicle and game.player.character.vehicle.name == "prt-pod" then

    --end

    --Handle Pod Logic here
    --TODO

    end
)